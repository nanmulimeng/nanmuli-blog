# <编号> <模块名> 排查报告

> **模块编号**：<B01/C01/F01/X01>
> **排查范围**：<一句话职责> + 关键文件清单（见下）
> **基线 commit**：<`git rev-parse HEAD` 的 SHA>
> **工作区状态**：<干净 / 脏（若脏，列出涉及本模块的未提交文件）>
> **排查日期**：YYYY-MM-DD
> **排查人**：<agent>
> **状态**：草稿 / 待复核 / 完成

---

## 模块概览

**职责**：<这个模块解决什么问题，一句话>

**关键文件**：
- `path/to/file.ext:行号` —— <作用>
- `path/to/file.ext:行号` —— <作用>

**对外接口 / 依赖**：
- 对外：<暴露的 Controller 路由 / API / 公共类>
- 依赖：<调用的其他模块 / 外部服务 / 配置 key / 表>

**已读文件清单**（可追溯 + 暴露盲区）：
- `<path>` —— <通读 / 片段 / 仅 grep>
- `<path>` —— ...

**主模块归属**：<若本模块是某共享对象的主模块（见计划 §8.6），列出并说明"深查"；否则注明对哪些共享对象"只引用">

---

## `[Bug]` 代码正确性与潜在缺陷

> 排查范围：<说明本节检查了哪些类/路径>。未命中则写"未发现"，并简述已检查范围。

### [P级别] [Bug] <简短标题>  <!-- 编号：<编号>-01 -->
- **定位**：`path/to/file.ext:行号`
- **现象**：<客观事实>
- **影响**：<真实使用/故障场景下的后果>
- **根因/分析**：<为什么，含已排除的误判>
- **修复方向**：<1-3 条方向，不写代码，标改动面 小/中/大（计划 §4.3）>
- **关联**：<[[模块-条目]] / 次维度标签 / 横向主题（§2.6）/ 配置项>

<!-- 每条发现复制本结构，编号递增 <编号>-02、<编号>-03 -->

---

## `[Security]` 安全漏洞

> 排查范围：<说明>。逐项覆盖计划 §2.2 技术栈重点（Sa-Token / MyBatis ${}vs#{} / Cookie+CSRF / CORS / AES / SSRF / 文件上传 / 双向 key）。未命中则写"未发现"。

### [P级别] [Security] <标题>  <!-- 编号：<编号>-NN -->
- **定位**：
- **现象**：
- **影响**：
- **根因/分析**：
- **修复方向**：（标改动面）
- **关联**：

---

## `[Arch]` 架构与技术债

> 排查范围：<说明>。未命中则写"未发现"。注意共享对象按 §8.6 归属，非主模块只引用编号。

### [P级别] [Arch] <标题>  <!-- 编号：<编号>-NN -->
- **定位**：
- **现象**：
- **影响**：
- **根因/分析**：
- **修复方向**：（标改动面）
- **关联**：

---

## `[Deps]` 依赖升级与版本

### 本模块依赖清单（基于声明文件，未翻依赖源码）

| 依赖 | 版本 | 声明位置 | 已知风险/可升级 | 备注 |
|---|---|---|---|---|
| <库名> | <当前版本> | `pom.xml` / `package.json` / `requirements.txt` | <CVE/废弃/可升至 X> | |

> 排查范围：<说明本节覆盖的依赖>。未命中则写"未发现"。

### [P级别] [Deps] <标题>  <!-- 编号：<编号>-NN -->
- **定位**：<依赖名 + 当前版本 + 声明位置>
- **现象**：
- **影响**：
- **根因/分析**：
- **修复方向**：（标改动面）
- **关联**：

---

## `[Design]` 功能设计合理性

> **必填**。从真实使用出发，回答计划 §2.5 中**相关**的问题（**至少 2 个**）。即便无 P0–P3 问题，也**必须**写 2–3 句审视结论，并显式标注 `无需调整` 或列为 `[Design/P4]` 建议。禁止留空或硬凑。

**审视结论**：

1. **<问题维度，如"场景适配">**：<观察 + 判断>
2. **<问题维度，如"闭环完整性">**：<观察 + 判断>
3. **<问题维度，如"可运维性/缺失功能/交互/单点扩展">**：<观察 + 判断>

### [P级别 / P4 / 无需调整] [Design] <标题>  <!-- 编号：<编号>-NN -->
- **定位**：<功能点 / 操作路径 / 配置>
- **现象**：<当前设计是什么>
- **影响**：<真实使用下的体验/运维后果>
- **建议方向**：<调整/补充/简化/维持>（标改动面）
- **关联**：

---

## 模块小结

### 严重度统计

| 级别 | 数量 | 条目编号 |
|---|---|---|
| P0 | 0 | — |
| P1 | 0 | — |
| P2 | 0 | — |
| P3 | 0 | — |
| P4 | 0 | — |

### Top 风险（本模块最该先看的 ≤3 条）

1. **<编号> <标题>** —— <一句话理由>
2.

### 修复优先级建议

- **立即**（P0/P1）：
- **计划**（P2）：
- **择机**（P3/P4）：

### 排查盲区 / 待复核

- <本次未能确认、需二次验证的点，同步登记到索引页 README.md 待复核清单>
